{
  "name": "redstone-smartweave-examples",
  "version": "0.1.0",
  "license": "MIT",
  "author": "Redstone Team <dev@redstone.finance>",
  "contributors": [
    "Piotr Pedziwiatr <peter@redstone.finance>"
  ],
  "scripts": {
    "benchmark:1": "ts-node --transpile-only src/benchmarks/ewe.benchmark.ts",
    "benchmark:2": "ts-node --transpile-only src/benchmarks/Daj.benchmark.ts",
    "benchmark:3": "ts-node --transpile-only src/benchmarks/Lpp.benchmark.ts",
    "benchmark:4": "ts-node --transpile-only src/benchmarks/-8A.benchmark.ts",
    "benchmark:5": "ts-node --transpile-only src/benchmarks/t9T.benchmark.mem.ar.ts",
    "benchmark:6": "ts-node --transpile-only src/benchmarks/t9T.benchmark.file.ar.ts",
    "benchmark:7": "ts-node --transpile-only src/benchmarks/t9T.benchmark.mem.redstonegw.ts",
    "benchmark:8": "ts-node --transpile-only src/benchmarks/t9T.benchmark.file.ar.current.ts",
    "benchmark:9": "ts-node --transpile-only src/benchmarks/t9T.benchmark.mem.ar.current.ts",
    "benchmark:10": "ts-node --transpile-only src/benchmarks/SJ3.benchmark.ts"
  },
  "dependencies": {
    "@kyve/query": "0.0.0-alpha.41",
    "arweave": "1.10.23",
    "redstone-smartweave": "0.4.47",
    "smartweave": "0.4.42",
    "ts-node": "^10.0.0",
    "tsconfig-paths": "^3.10.1",
    "typescript": "^4.3.5"
  },
  "devDependencies": {
    "@types/node": "^14.14.28",
    "cli-table": "0.3.11",
    "colors": "^1.4.0",
    "simple-statistics": "^7.7.0"
  }
}
